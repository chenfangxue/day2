#include <stdio.h>

int main()
{

    printf("进入存钱取钱管理系统\n");

    return 0;
    int qian = 0;
    int code;
    int cun;
    int jine;
    if (code == 1)
    {
        printf("请输入要存入的金额\n"); 
        scanf("%d",&cun);
        qian+=jine;
        printf("存入成功，点击回车继续\n");
        scanf("%c",&x);
        scanf("%c",&x);
    }
    if (code == 2)
    {
        printf("请输入要取出的金额\n");
    }
    if (code == 3)
    {
    }
    if (code == 4)
    {
    }
}